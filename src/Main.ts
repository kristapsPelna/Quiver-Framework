import {metadata} from "./metadata/metadata";
import {CommandMap} from "./commandMap/CommandMap";
console.log("Quiver Framework is running");

console.log('>> ', metadata.getTypeDescriptor(CommandMap));